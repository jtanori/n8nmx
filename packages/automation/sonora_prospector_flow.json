{
  "name": "Sonora Prospector Flow",
  "nodes": [
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT t.id as task_id, t.term, l.city_name, l.lat as latitude, l.lng as longitude \nFROM search_queries t \nJOIN search_locations l ON t.id = l.search_id \nWHERE t.is_executed = FALSE \nORDER BY t.priority_order ASC, t.created_at ASC \nLIMIT 1;"
      },
      "id": "get_next_task",
      "name": "Get Next Pending Task",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.2,
      "position": [300, 300]
    }
  ],
  "connections": {}
}
